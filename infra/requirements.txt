aws-cdk-lib>=2.160.0,<3.0.0
constructs>=10.0.0,<11.0.0
# Pinned exactly: cdk-nag 3.0.2 (the latest as of this writing) throws a
# jsii "aspect.visit is not a function" error against recent aws-cdk-lib
# versions. 2.38.2 is the version this project was actually built and
# tested against — bump deliberately, not via a loose range.
cdk-nag==2.38.2
